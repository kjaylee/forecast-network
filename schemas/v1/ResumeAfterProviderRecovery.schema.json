{
  "$comment": "Structural contract. Decode through the domain boundary to enforce semantic invariants.",
  "$defs": {
    "ResumeAfterProviderRecovery": {
      "additionalProperties": false,
      "properties": {
        "kind": {
          "const": "resume_after_provider_recovery"
        },
        "recovered_provider": {
          "maxLength": 1000000,
          "minLength": 1,
          "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}(?![\\s\\S])",
          "type": "string"
        },
        "schema_version": {
          "const": 1,
          "maximum": 9007199254740991,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "schema_version",
        "recovered_provider",
        "kind"
      ],
      "type": "object"
    }
  },
  "$id": "https://schemas.forecast.network/v1/ResumeAfterProviderRecovery.schema.json",
  "$ref": "#/$defs/ResumeAfterProviderRecovery",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ResumeAfterProviderRecovery"
}
