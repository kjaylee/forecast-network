# Forecast quality extension

Mode: Operate. Target: `apps/web/public/app.js` feed, detail and current profiles.

Preserve the established light editorial feed, cobalt actions, system type, dividers and question-first hierarchy. Extend existing probability comparisons to four independent groups. Use a two-column mobile comparison rather than shrinking four columns. Show quality explanations as native disclosures, and profile quality as a compact definition list and category record rows. Display absent/provisional samples honestly; demonstrated forecasting expertise never implies professional credentials. Preserve existing share-card controls, published v1 snapshots, wallet and points flows. Four locales are required.

Verification: one batched desktop/mobile inspection with real SQLite-derived API snapshots, one corrective pass only if needed. Screenshots and temporary local-server artifacts live in `tmp/quality-ui`.
