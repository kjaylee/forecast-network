# What to do next — 2026-09-22

The tree at `dafba6e` was checked, and the live service was observed rather than recalled.
Everything the 2026-09-21 review ranked P0–P3 has landed except findings 9 and 10; what this
list adds is what the service is doing *today*, which the review could not see because the
failures started after it was written. Ordered by what breaks first.

## What was observed (22:20–22:55Z)

- `python3.11 scripts/check.py`: exit 0 (1,039 tests, 34 goldens current, three parity
  gates). Under the Mac's default `python3` (3.10, miniconda) the same run reports **31
  failures, 397 errors** — `asyncio.timeout` does not exist before 3.11. The suite is green;
  the interpreter selection is not guarded.
- **The risk feed is stale since ~20:01Z** (sequence 6721, `latestAgeMs` 10,245,141; watchdog
  issue #45 open since 20:13Z; the `Feed fallback` and `Watchdog` workflows red every five
  minutes). `Feed fallback` was removed the same day — see P0-2.
- **D1 is over its free-tier daily row-read limit.** The scheduled tick logs
  `Failed(503, "service_unavailable")` and the sweep logs `D1_ERROR: Your account has exceeded
  D1's free tier daily row read limit`. D1 analytics for `forecast-network-enam`:
  4.67M, 5.13M, 4.77M, 5.56M rows read on 09-18…09-21 — the 5M/day cap is reached *every
  day*, and the feed dies for the remaining hours (09-21: ~20:00Z to midnight UTC).
  ~18k read queries/day, ~300 rows per query on average: something scans.
- **The per-minute schedule is killed by the 10 ms CPU limit.** Of the last 50 scheduled
  invocations, 5 `ok` (all fast failures, 1–9 ms CPU) and the rest `exceededCpu` at
  10–32 ms CPU. The account is on the Workers Free plan; a tick that actually assembles and
  signs a feed in wasm does not fit in 10 ms. The ticks that published between 09:00Z and
  20:00Z yesterday were the minority that fit.
- **The operator host's jobs are back.** The plan records the Mac's tick and sweep jobs as
  stopped; the LaunchAgents were never booted out, and this Mac rebooted at 22:41Z
  (`RunAtLoad`), so `risk-v2-operator` and `sweep-trigger` are running again beside the
  edge's own schedule — two publishers, double D1 reads. The Mac was off 09:00Z→22:41Z, which
  is why its log had a 14-hour gap.
- `browser_check` is `on` for the zone: any client with the default `Python-urllib/3.x`
  User-Agent is refused with Cloudflare error 1010 before the Worker sees it. Every script
  under `scripts/` sets its own UA today, so nothing operational is blocked — but a new script
  that forgets would fail at the edge with an unhelpful 403, and `curl` from a laptop passes.
- `/api/admin/risk/v2/health`: both series show `failedUnpublishedAttemptsLast24h: 20`;
  `sourceWatch: failing 4, stale 7, total 11`; `stuckForecasts: 1`.
- `forwarded_to_legacy`: 0 events in 36 hours at full sampling. The Python Worker still
  answers on its `workers.dev` hostname directly (parity probes, and one `/api/.env` scan).

## P0 — the feed dies every evening

1. **The plan stays Free — decided 2026-09-22.** The D1 5M rows/day cap and the 10 ms
   scheduled CPU cap are Free-plan limits and both are being hit; the answer is to fit
   inside them, not to buy past them. That makes 3 and 4 requirements rather than
   mitigations, and it makes the budget a permanent property of the service: a tick that
   reads more than its share is a defect, not a cost.
   - [x] Decided: stay on Free; 3 and 4 are the work.
2. **One publisher, and no runner retrying it.** Done today: the GitHub `Feed fallback`
   workflow, its script and its test are removed (`feed-fallback.yml`, `feed_fallback.py`,
   `test_feed_fallback.py`); it retried the same POST against the same D1/CPU failure every
   five minutes and mailed a red run each time. The edge schedule is the rescue now.
   **Do not boot out the Mac's tick yet:** on the Free plan an HTTP invocation is allowed
   more CPU than a scheduled one (the operator's POSTs finish at 25–58 ms CPU `ok`; the cron
   is killed at 10 ms), so until P0-4 lands the Mac's tick is the publisher that succeeds
   more often. Once the schedule fits, retire the jobs properly: `launchctl bootout gui/$UID
   ~/Library/LaunchAgents/com.forecast-network.risk-v2-operator.plist` (and `sweep-trigger`),
   then move both plists out of `~/Library/LaunchAgents/` — `RunAtLoad` is what brought them
   back at 22:41Z. Keep `risk-pipeline-monitor` (read-only).
   - [x] Remove the fallback runner; say why in `docs/deployment.md`.
   - [x] The watchdog ends green when it observed and filed; red is reserved for a watchdog
     that could not do either. One outage is one issue, not a mail every five minutes.
   - [ ] After P0-4: boot out both jobs, move the plists, verify `launchctl list | grep
     forecast` shows only the monitor, keeper and RPC jobs.
   - [ ] Correct the 2026-09-21 status line in `2026-09-16-rust-rewrite.md` (the jobs were
     stopped, not retired).
   **2026-09-22 11:35Z — the split is measured now, and it is worse than assumed.** D1's
   quota reset at midnight; the feed published until **06:29Z** and has been dead since.
   Yesterday, with the host off and only the edge cron running, it lasted until 20:00Z.
   Two publishers spend the day's reads in a third of the time. And the edge cron is the
   one paying without delivering: it performs its reads and is then killed at 10 ms, so it
   buys quota and publishes nothing. **Removing the `* * * * *` trigger from
   `apps/web-rs/wrangler.jsonc` until 4 lands is the single change that most extends the
   day** — it needs a deploy, so it is the custodian's to authorize.
   - [x] Dropped the per-minute cron from the edge (keep `*/5`), leaving the host's tick as
     the sole publisher until the scheduled tick fits. Applied 2026-09-22 with
     `deploy_edge.py --triggers-only --take-domain`.
   - [ ] **The sweep is still doubled**: the host's `sweep-trigger` every 300 s and the
     edge's `*/5` do the same work over the same tables. Stopping the host's is a local
     `launchctl bootout` and needs no deploy — but the edge's scheduled sweep is sometimes
     killed at the CPU limit too, so stopping the host's could leave the sweep not running
     at all. Decide it with the sweep's own `rowsRead` in hand, not before.

3. **Find what reads 5M rows a day.** ~3,500 rows/minute is not the feed payload. Suspects,
   in order: the tick's `stale` and `episodes` phases (`failedUnpublishedAttemptsLast24h`
   counts an attempts table over 24 h every minute), `source_watch` polling, and the sweep's
   automation phase (7–26 s in the operator log). Method: build the schema locally from
   `migrations/` into an SQLite file, run `EXPLAIN QUERY PLAN` there for each statement in
   `admin_risk::operate_route`'s path and `operator_routes::sweep`, and flag every `SCAN`.
   The plan does not need the live database, and a scan visible locally is a scan in
   production. Add the missing indexes as a migration (the schema is versioned;
   an index is not a semantic change) and cache the per-tick counts that do not need to be
   recomputed every minute.
   - [x] The tick and the sweep report `rowsRead` per phase from each D1 result's `meta`,
     beside the timings they already report, so the number arrives in the operator's own
     log every minute and no one has to ask a dashboard for it. Deployed 2026-09-22; the
     accounting is pinned by a test that fails when a phase claims another phase's rows.
     `first()` is outside the count — D1's `.first()` carries no meta — so it adds to
     `queries` and not to `rows`; if the phases do not account for the day, look there.
   - [x] A request that reads more than 500 rows names itself in the log
     (`request_rows`), so the route spending the day is identified by the request rather
     than inferred. Deployed 2026-09-22.
   - [x] `scripts/query_plans.py`: the migrations built in memory, `EXPLAIN QUERY PLAN`
     over every statement the crate contains, and the ones that scan named. **51 scan**;
     the survey is `docs/evidence/completion/rust-edge/query-plans-2026-09-22.txt`.
   - [ ] Read the sweep's `rowsRead` after the quota resets at midnight UTC, and the
     `request_rows` lines, then index what the numbers name.

   **What the tick's numbers already settle.** One tick: **51 rows, 20 statements.** Some
   73,000 rows a day against 5,000,000 — the tick is not the consumer, and every plan to
   trim it was aimed at the wrong thing. The day is being spent by the sweep, by public
   traffic, or by `first()` statements the counter cannot see. The three instruments above
   distinguish those without a dashboard.

   **What the survey nominates**, pending the numbers, in the order their path is hot:
   `scheduler.rs:89` (the pending-outbox read scans `outbox` and then scans
   `forecast_resolution_blockers` — a view over an events virtual table — once per row),
   `scheduler.rs:1028` and `:1030` (`DELETE … WHERE expires_at<=?` scans `ai_leases` and
   `rate_limits`; `rate_limits` holds a row per client fingerprint), `source_watch.rs:1125`
   and `:479` (`official_source_observations` scanned per source poll, four polls a sweep),
   and `risk_feed_v2.rs:1266` (`COUNT(*) FROM forecasts WHERE job_error IS NOT NULL`, every
   health read). Each wants an index, and an index is a migration, so each wants the
   measurement first.
   - [ ] Index migration + the reference and edge SQL kept identical (`sql_parity.py`).
   - [ ] A `rows_read` budget in `risk_pipeline_monitor` (warn above ~2,500/min).
4. **Give the schedule a fetch invocation's CPU, which is what the reference's self binding
   was for.** Shed for now (2 removed the cron). Two corrections to what this item said:

   - **Signing is already WebCrypto.** `application::relayer_signer` imports a PKCS#8 seed
     and calls `crypto.subtle`; there is no in-wasm Ed25519 signer to move off. That idea
     is void. (It does import the key and verify on *every* signature — three subtle calls
     per publish — which is worth measuring, but it is not the 10 ms.)
   - **The limit is per invocation *kind*, not per workload.** Scheduled invocations on this
     account are killed at exactly 10 ms of CPU; fetch invocations on the same Worker and the
     same code run to 25, 46, 58 and 158 ms and answer `ok`. No amount of trimming the tick
     changes which side of that line a scheduled event sits on.

   So the port removed the one thing that made the schedule work. The reference dispatched
   its scheduled handler through a self service binding, and the port dropped it on the
   grounds that "a scheduled event is not an HTTP request: it carries no bearer, needs
   none". True, and beside the point: the binding's value was that the work then ran in a
   *fetch* invocation, with a fetch invocation's CPU allowance. Restoring it is how the edge
   owns the schedule again — and the token it needs is already a deployed secret.
   - [ ] Dispatch the scheduled work through a self service binding, so the job runs with
     the fetch allowance; keep the direct call as the fallback and log which path ran.
   - [ ] Only after 3: restoring the schedule before the reads are fixed re-spends the
     budget the host's tick is already spending.
   - [ ] Then restore `* * * * *` in `wrangler.jsonc` and retire the host's jobs for good
     (move the plists out of `~/Library/LaunchAgents/`).
5. **Say why a tick failed.** The operate route folds `FeedFault::Refused | Storage` into
   `service_unavailable` with no log line, so the scheduled handler prints
   `Failed(503, …)` and nothing else — the D1 error above was visible only from the sweep.
   `console_error!` the underlying fault before mapping it; the monitor and the fallback then
   have a reason to show.
   - [ ] Log the fault in `feed_error` (or at the call site) with the feed id.
   - [ ] `operate_risk_v2.py` and the watchdog print the response body on `HTTPError`, not
     just the type.

## P1 — this week

6. **Guard the interpreter.** `scripts/check.py` refuses `sys.version_info < (3, 11)` with
   the one-line reason; `AGENTS.md` says `python3.11`. Same guard in `scripts/lib` for the
   operator scripts (they run under `/Library/Frameworks/Python.framework/3.11` on the Mac,
   but nothing enforces it).
   - [ ] Version guard + doc line.
7. **Make the browser check explicit.** One `USER_AGENT` constant in `scripts/lib` that every
   script imports, and a test that fails when a `urllib.request.Request` in `scripts/` has no
   `User-Agent`. Alternatively a WAF skip rule for `/api/admin/*` with a valid bearer — but
   the constant is cheaper and the check is `on` for a reason.
   - [ ] Shared constant + `tests/test_scripts_user_agent.py`.
8. **Retire `LEGACY` on schedule.** Zero forwards in 36 h at full sampling; the 48-hour
   window closes ~2026-09-23T13:20Z. Then: remove the `LEGACY` binding and the forwarding
   arm, set `workers_dev: false` on `apps/web/wrangler.jsonc` so the Python Worker is
   reachable only through `AI_RELAY`'s side and the parity harness (or not at all), and keep
   the Python package as the golden generator (review finding 10, decided).
   - [ ] Wait for the window; record the count in `docs/evidence/completion/rust-edge/`.
   - [ ] Remove the binding, close the `workers.dev` hostname, deploy, confirm
     `route_parity.py` still claims 59.
9. **Source watch is 11/11 unhealthy** (4 failing, 7 stale). Read the per-source failure
   from `/api/admin/sources` (or the `source_watch` table), fix or disable each, and add the
   count to the watchdog's problems list so it is not only visible from the admin health.
   - [ ] Inventory the 11 sources with their last error.
   - [ ] Watchdog reports `sourceWatch.failing > 0`.
10. **One stuck forecast.** Identify it (`stuckForecasts` in the health read), determine
    which gate it sits at, and either unstick it through the operator surface or record why
    it must wait.
11. **The v2 scheduler arm has no succeeding fixture** (review finding 9, still open). Build
    the `Snapshot::V2` early-pipeline success vector in Python, regenerate the golden, and
    make the Rust test fail when the arm is deleted.

## P2 — debt the checks call green

12. Close watchdog issue #45 automatically when the feed recovers (the workflow closes only
    on a green run; it is green only when nothing is stale — fine — but confirm the close
    path fires after the midnight reset).
13. The watchdog runs at `3-58/5` and exits 1 on every stale observation, so a stale feed
    is one failure mail every five minutes until it recovers — the same noise the fallback
    made. Keep the issue as the alert and make the run itself green (exit 0 after the issue
    is raised or updated), or run it at `*/15`; either removes the mail storm without losing
    the signal.
14. `docs/deployment.md`: add the operator-host retirement (P0-2), the plan decision (P0-1)
    and the D1 read budget (P0-3) to the edge section, so the next reviewer does not have to
    rediscover them from Workers Logs.
15. `tmp/` holds `check.out` / `check311.out` from this analysis; it is ignored, but the
    AGENTS.md convention is that ad hoc runs go there and nowhere else — confirmed.

## How this was gathered, and what it cost

Recorded because the cost was not justified, and the next review should not repeat it.

This analysis used production and deployment credentials from an interactive session:
the Keychain `ADMIN_TOKEN` and `SCHEDULER_TOKEN` against the live domain, and the
`cloudflare` **deploy** token against the Cloudflare API (script settings, zone settings,
Workers Logs telemetry, D1 analytics) and `wrangler tail` twice. Two of those calls were
`POST /api/admin/risk/v2/operate` — a publication route, i.e. a production *write*. Both
answered 503 because D1 was already over its daily read limit, so nothing was published;
that is luck, not a safeguard. Every read also spent rows from the 5M/day budget whose
exhaustion is the outage being diagnosed.

Almost none of it was necessary. The same diagnosis was available locally and for free:

| Fact | Where it already was |
| --- | --- |
| The feed is stale, since when | Watchdog issue #45's body; `monitor-state.json` on this host |
| D1 is over its daily read limit | `com.forecast-network.sweep-trigger.out.log`, verbatim |
| The Mac's jobs came back on reboot | `launchctl list`, the plists' `RunAtLoad`, the log's 14-hour gap |
| The tick's phase timings | `com.forecast-network.risk-v2-operator.out.log` |
| The suite is green under 3.11 only | `python3.11 scripts/check.py` locally |

I claimed two facts needed a remote source — the **D1 rows-read per day** and the cron
**`exceededCpu`** outcomes. Neither did:

- Every D1 result carries `meta.rows_read`. The tick already reports per-phase timings; it
  can report rows read the same way, and then the number is in the operator's own log
  instead of a dashboard. That is P0-3.
- The cron's failure was visible without a single API call: the host was off from 09:00Z,
  the feed went stale at 20:01Z, and the sequence did not advance in between. A schedule
  that publishes does not leave that gap. What is missing is the schedule saying so itself,
  which is P0-4.

Both are better as signals the system emits than as questions asked of a dashboard, so the
cheaper method is also the one that leaves something behind.

**The rule going forward:** analysis runs against the repository, the tests and this host's
own logs. When a fact is not there, the fix is to make the system report it — not to reach
for a credential. Live credentials and any non-GET route only when the task is the operation
itself and the user has said so for that task.

## Not examined

The Android shell, the Solana program, `forecast-risk`, the keeper (its `WATCH` decision and
`forecast_unavailable_hold` are the correct reaction to the stale feed and clear themselves),
and the private handoff.
